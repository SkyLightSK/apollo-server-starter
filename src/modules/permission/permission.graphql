scalar JSON

type Query {
  getPermissions: [Permission]
#  cat(id: ID!): Permission
}

#type Mutation {
#  createPermission(createPermissionInput: CreatePermissionInput): Permission
#}
#
#type Subscription {
#  catCreated: Permission
#}

type Permission {
  id: Int
  title: String
  alias: String
  conditions: JSON
  createdAt: String
  updatedAt: String
  roles: [Role]
}

input CreatePermissionInput {
  title: String
  alias: String
  createdAt: String
  updatedAt: String
}
