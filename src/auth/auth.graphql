#type Query {
#  getCats: [Cat]
#  cat(id: ID!): Cat
#}

type Mutation {
  login(authUserInput: LoginUserInput): Auth
}

type Auth {
  email: String
  family_name: String
  given_name: String
  hd: String
  id: String
  id_token: String
  locale: String
  name: String
  picture: String
  verified_email: Boolean
}

input LoginUserInput {
  code: String
}

#type Subscription {
#  catCreated: Cat
#}
